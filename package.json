{
  "name": "backend-seed",
  "scripts": {
    "start": "npm run clean && tsc && webpack && concurrently \"tsc -w\" \"node bundle/backend.js\"",
    "clean": "rm -rf dist/ && rm -rf build/"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "concurrently": "^3.4.0",
    "express": "^4.12.2",
    "sequelizer": "^1.1.3",
    "socket.io": "^1.3.5",
    "source-map-support": "^0.4.14",
    "sqlite3": "^3.1.8",
    "webpack": "^1.7.3",
    "webpack-dev-server": "^1.7.0",
    "webpack-merge": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^7.0.5"
  }
}
